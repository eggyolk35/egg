services:
  web:
    build: .
    ports:
      - "3000:3000"       # 左边可改成你想要的外部端口，如 "80:3000"
    volumes:
      - ./data:/app/data  # SQLite 数据库持久化（不挂载则重启后记录丢失）
    restart: unless-stopped
    environment:
      NODE_ENV: production
